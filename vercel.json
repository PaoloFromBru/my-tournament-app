{
  "routes": [
    {
      "src": "/api/stripe-webhook",
      "methods": ["POST"],
      "headers": {
        "Access-Control-Allow-Origin": "*"
      },
      "dest": "/api/stripe-webhook"
    }
  ]
}
